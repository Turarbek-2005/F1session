# F1KZ - Expo Mobile Application

Мобильное приложение для отслеживания данных Формулы 1 (пилоты, команды, результаты) с интеграцией F1 API.

---

## 🎓 ДЛЯ ПОКАЗА В УНИВЕРСИТЕТЕ

**👉 Читайте:** [QUICK-START.md](./QUICK-START.md) - быстрый старт  
**👉 Чеклист:** [CHECKLIST.md](./CHECKLIST.md) - что проверить перед показом  
**👉 Детали:** [UNIVERSITY_SETUP.md](./UNIVERSITY_SETUP.md) - полная инструкция

### Быстрые команды:

```bash
# Проверить готовность
check-ready.bat

# Экспорт базы данных (на основном ноутбуке)
export-db.bat

# Импорт базы данных (на втором ноутбуке)
import-db.bat

# Запустить для университета
start-for-university.bat
```

---

- **App.js** - Главный файл с навигацией и Redux Provider
- **screens/** - Все экраны приложения
  - HomeScreen.tsx - Домашний экран с навигацией
  - DriversScreen.tsx - Список пилотов F1
  - TeamsScreen.tsx - Список команд F1
  - store.ts - Redux store
  - driversSlice.ts - Redux slice для пилотов
  - teamsSlice.ts - Redux slice для команд
  - axios.ts - Настройка API клиента
  - types.ts - TypeScript типы
  - hooks.ts - Redux хуки

## Установка и запуск

### 1. Установите зависимости:

```bash
npm install
```

Основные зависимости:
- @reduxjs/toolkit - для управления состоянием
- axios - для API запросов
- react-redux - для интеграции Redux с React
- @react-navigation/native - для навигации
- expo - для разработки мобильного приложения

### 2. Настройте API URL

Отредактируйте файл `screens/axios.ts` и замените `baseURL` на адрес вашего backend сервера:

```typescript
const baseURL = "http://192.168.1.100:3000"; // Замените на ваш IP адрес
```

**Важно:** 
- Для тестирования на физическом устройстве используйте локальный IP адрес вашего компьютера (не localhost)
- Убедитесь, что устройство и компьютер находятся в одной сети
- Backend сервер должен быть запущен и доступен

### 3. Запустите приложение:

```bash
npm start
```

Затем выберите платформу:
- `a` - для Android
- `i` - для iOS (только на macOS)
- `w` - для веб версии

## API Endpoints

Приложение использует следующие endpoints:

- `GET /f1/drivers` - Получить список всех пилотов
- `GET /f1/teams` - Получить список всех команд

Убедитесь, что ваш backend сервер реализует эти endpoints.

## Возможности

- 📱 Адаптивный дизайн для мобильных устройств
- 🔄 Redux для управления состоянием
- 🚀 Асинхронная загрузка данных
- 🎨 Стилизация в стиле F1
- 📊 Отображение пилотов и команд с изображениями
- ⚡ Быстрая навигация между экранами

## Структура данных

### Driver
```typescript
{
  id: number;
  driverId: string;
  imgUrl: string;
  teamId: string;
  nationality: string;
  nationalityImgUrl: string;
  createdAt: string;
  updatedAt: string;
}
```

### Team
```typescript
{
  id: number;
  teamId: string;
  teamImgUrl: string;
  bolidImgUrl: string;
  createdAt: string;
  updatedAt: string;
}
```

## Troubleshooting

### Не загружаются данные
1. Проверьте, что backend сервер запущен
2. Убедитесь, что URL в `screens/axios.ts` правильный
3. Проверьте консоль на наличие ошибок сети

### Проблемы с изображениями
- Убедитесь, что URL изображений доступны
- Проверьте, что сервер возвращает правильные URL

### Ошибки при установке
- Попробуйте удалить `node_modules` и `package-lock.json`
- Запустите `npm install` снова
- Убедитесь, что у вас установлена актуальная версия Node.js

## Разработка

Для добавления новых функций:

1. Создайте новый slice в папке `screens/`
2. Добавьте его в store в `screens/store.ts`
3. Создайте новый экран в `screens/`
4. Добавьте маршрут в `App.js`

## Конвертация из Next.js

Это приложение было создано из Next.js frontend со следующими изменениями:

- Next.js `Link` → React Navigation
- Next.js `Image` → React Native `Image`
- Tailwind CSS → React Native StyleSheet
- Web компоненты → React Native компоненты
- Next.js API routes → Axios запросы к отдельному backend

© 2025 F1KZ. All rights reserved.
